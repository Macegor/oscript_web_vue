Функция Index() Экспорт // BSLLS:UnusedLocalMethod-off

	ТекстовыйДокумент = Новый ТекстовыйДокумент;
	ТекстовыйДокумент.Прочитать("./wwwroot/index.html");
	ТекстHtml = ТекстовыйДокумент.ПолучитьТекст();

	ОбъектМодели = Новый Структура("includedText", ТекстHtml);
	Возврат Представление(ОбъектМодели);	

КонецФункции